[pytest]
testpaths = tests
pythonpath =
    .
    src
addopts = -ra -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore:pkg_resources is deprecated as an API.*:UserWarning:mlflow\.utils\.requirements_utils